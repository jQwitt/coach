import { Inter as Base, Bebas_Neue as Heading } from "next/font/google";

export const base = Base({
	subsets: ["latin"],
	display: "swap",
	weight: "300",
});

export const heading = Heading({
	subsets: ["latin"],
	display: "swap",
	weight: "400",
});
