import type { LiveCoachFulfillmentFunction } from "@/lib/types/live-coach";

export const designWorkout: LiveCoachFulfillmentFunction<null> = async ({
	muscleGroup,
	exercise,
}) => {
	return null;
};
