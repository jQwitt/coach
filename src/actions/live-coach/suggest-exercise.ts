import type { LiveCoachFulfillmentFunction } from "@/lib/types/live-coach";

export const suggestExercise: LiveCoachFulfillmentFunction<null> = async ({
	muscleGroup,
	exercise,
}) => {
	return null;
};
